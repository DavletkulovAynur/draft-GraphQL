import { Button, CircularProgress } from "@mui/material";

function FormButton() {
  return (
    <Button
    type="submit"
    size="large"
    
    fullWidth
    variant="outlined"
    aria-label="login"
  >
    ВОЙТИ
  </Button>
  )
}

export default FormButton